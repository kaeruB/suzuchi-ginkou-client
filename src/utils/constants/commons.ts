export const DATE_PARTS_SEPARATOR = '-'

export const IMG_PATHS = (avatarName: string) => `/images/${avatarName}.png`

export enum Currency {
  PLN = 'PLN',
  YEN = 'YEN',
}

export const PAIR_ID_SEPARATOR = '_'
